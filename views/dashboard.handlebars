<div class="text-center text-primary mt-5" style="font-size: xx-large; font-weight: 600;">Youtube to Mp3 Api</div>
<div class="text-center text-primary mt-5" style="font-size: x-large; font-weight: 600;"><span
        class="text-danger">{{Name}}</span>'s Api details</div>


<div class="container mt-5 w-70 align-self-center text-center">
    <table class="table table-striped table-bordered" style="border:black 2px solid;">
        <thead class="thead-dark">
            <tr>
                <th scope="col">Name</th>
                <th scope="col">Email</th>
                <th scope="col">Max Requests</th>
                <th scope="col">Allowed IP</th>
                <th scope="col">Api Key</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{Name}}</td>
                <td>{{Email}}</td>
                <td>{{MaxReq}}</td>
                <td><code>{{IP}}</code></td>
                <td>
                    <button class="btn" id="apibtn" onclick="RevealAPI()" data-toggle="tooltip" data-placement="right"
                        title="Click to reveal">
                        <code>**********************************</code>
                    </button>
                    <div class="collapse" id="apikey"><code>{{ApiKey}}</code></div>
                </td>
            </tr>
        </tbody>
    </table>
</div>
<div class="text-center mt-2" style="font-size: large; font-weight: 600;">In order to access the api, use the
    following link:<br>
    <span id="PageUrl"></span></span>api/@apikey/@VideoId<br>
</div>


<div class="mt-2" style="font-size: small; font-weight: 600; padding-left:30%">
    <span class="text-primary" style="font-size: medium;">Instructions:</span><br>
    1. Replace @apikey with your API Key and @VideoId with the youtube video id<br>
    2. Make sure to connect with the Allowed IP otherwise the connection will be rejected <br>
    3. If you have any problems, please contact Umar on discord(code: <code style="font-size: medium;">Umar#2781</code>)
</div>


{{#if ReqExists}}

<div class="text-center text-primary mt-5" style="font-size: x-large; font-weight: 600;">File Links</div>
<div class="container mt-2 w-25 align-self-center text-center">
    <table class="table table-striped table-bordered" style="border:black 2px solid;">
        <thead class="thead-dark">
            <tr>
                <th scope="col">Name</th>
                <th scope="col">Link</th>
            </tr>
        </thead>
        <tbody>
            {{#each members}}
            <tr>
                <td>{{this.file}}</td>
                <td><a href="{{this.Path}}{{this.file}}">Get Link</a></td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</div>
{{/if}}



<div class="mt-5 text-center" style="font-size: medium; font-weight: 600;">
    <a href="/dashboard/ChangePass">Click here to change your password</a>
</div>


<script>
    function RevealAPI() {
        document.getElementById('apibtn').style.display = 'none';
        document.getElementById('apikey').classList.add('collapse.show');
        document.getElementById('apikey').classList.remove('collapse');
    }
    document.getElementById('PageUrl').innerText = window.location.protocol + '//' + window.location.host + '/';
</script>